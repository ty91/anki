export type Rating = "again" | "hard" | "good" | "easy";

export type StudyItem = {
  id: number;
  expression: string;
  meaning: string;
  examples: string[];
  toneTip: string;
  etymology: string;
};

